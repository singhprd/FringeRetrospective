services:
  fringeretrospective:
    build: .
    environment:
      DATABASE_HOST: db
    ports:
      - "3000:3000"
    depends_on:
      - db
  db:
    image: mysql:8.0
    restart: always
    # environment:
      # POSTGRES_USER: db_user
      # POSTGRES_PASSWORD: password
      # POSTGRES_HOST_AUTH_METHOD: trust
